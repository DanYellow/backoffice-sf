<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% block titleTab %}Backoffice Caroleoreb.fr{% endblock %}</title>
    
  {% block stylesheets %}
   
    <link href="{{ asset("bundles/backendadmin/stylesheets/main.css") }}" rel="stylesheet" type="text/css" />
  {% endblock %}

  <script type="text/javascript">
  var App = {
    environment: "prod"
  }
  </script>
</head>
<body>
    {% if app.user %}
    <header id="main-header">
        <div class="container">
            <div class="row">
                <div class="col-xs-6">
                    <span>Bonjour {{ app.user.firstname }}</span>
                </div>
                <div class="col-xs-6 text-right">
                  <p>
                    <a href="{{ path('fos_user_security_logout')}}">Déconnexion</a>
                  </p>
                </div>
            </div>
        </div>
    </header>
    {% endif %}
    
    <div class="container"> 
        {% block content %}{% endblock %} 
    </div>
    
    <script src="{{ asset("bundles/backendadmin/javascripts/bundle.js") }}"></script>
</body>
</html>
